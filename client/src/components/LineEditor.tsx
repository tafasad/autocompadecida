import type { ScriptLine } from "@/lib/types";interface LineEditorProps {  line: ScriptLine | undefined;  onUpdate: (patch: Partial<ScriptLine>) => void;  onPlayEffect: () => void;  onFileUpload: (file?: File) => void;}export default function LineEditor({ line, onUpdate, onPlayEffect, onFileUpload }: LineEditorProps) {  if (!line) {    return (      <div className="flex items-center justify-center h-full text-white/20 text-sm">        Selecione ou crie uma fala      </div>    );  }  return (    <div className="space-y-5 p-5">      <div className="rounded-2xl border border-[#00f0ff]/10 bg-[#0d0d15] p-5">        <label className="text-[10px] uppercase tracking-[0.24em] text-[#00f0ff]/50 font-mono">          Texto da fala        </label>        <textarea          value={line.text}          onChange={e => onUpdate({ text: e.target.value })}          placeholder="Ex.: E ande logo antes que mudem de ideia!"          className="mt-3 min-h-36 w-full resize-y rounded-xl border border-[#00f0ff]/10 bg-black/60 p-4 text-xl font-semibold leading-relaxed text-white outline-none transition placeholder:text-white/20 focus:border-[#00f0ff]/40"        />      </div>      <div className="grid gap-4 md:grid-cols-2">        <div className="rounded-2xl border border-[#00f0ff]/10 bg-[#0d0d15] p-5">          <label className="text-[10px] uppercase tracking-[0.24em] text-[#00f0ff]/50 font-mono">            Nome do efeito          </label>          <input            value={line.effectName}            onChange={e => onUpdate({ effectName: e.target.value })}            placeholder="Risada, aplauso, suspense..."            className="mt-3 w-full rounded-xl border border-[#00f0ff]/10 bg-black/60 px-4 py-3 text-sm text-white outline-none transition focus:border-[#00f0ff]/40 placeholder:text-white/20"          />        </div>        <div className="rounded-2xl border border-[#00f0ff]/10 bg-[#0d0d15] p-5">          <label className="text-[10px] uppercase tracking-[0.24em] text-[#00f0ff]/50 font-mono">            Arquivo de som          </label>          <input            type="file" accept="audio/*,video/*"            onChange={e => onFileUpload(e.target.files?.[0])}            className="mt-3 w-full rounded-xl border border-dashed border-[#00f0ff]/20 bg-black/60 px-4 py-3 text-xs text-white/50 file:mr-3 file:rounded-full file:border-0 file:bg-[#00f0ff]/20 file:px-3 file:py-1.5 file:text-[10px] file:font-bold file:text-[#00f0ff]"          />          <p className="mt-2 text-[10px] text-white/30 font-mono">            {line.audioName ?? "Sem arquivo: usa risada sint├®tica ou beep"}          </p>        </div>      </div>      <div className="flex flex-wrap gap-2">        <button          onClick={onPlayEffect}          className="rounded-full bg-gradient-to-r from-[#00f0ff] to-[#7c3aed] px-5 py-2.5 text-xs font-bold text-black transition hover:opacity-90"        >          Testar efeito        </button>      </div>    </div>  );}